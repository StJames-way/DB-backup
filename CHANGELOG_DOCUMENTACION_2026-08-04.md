# Cambios documentales 2026-08-04

- arquitectura Cloudflare Worker + VPC + Tunnel + Flycast;
- verificación OIDC en Worker y signer;
- resiliencia JWKS y fail-closed;
- Supabase TLS `verify-full` y CA pinneada;
- rol `backup_reader` con connlimit 10 y timeouts;
- escala a cero del signer;
- nombres `aaa/aab` y JSON en rutas distintas;
- run exitoso `30878970809` como evidencia;
- guía de portabilidad y recovery drill;
- troubleshooting con incidencias reales de puesta en marcha.
